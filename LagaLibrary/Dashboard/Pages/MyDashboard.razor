@page "/MyDashboard"

@using ChartJs.Blazor.PieChart
@using System.Drawing;

<Chart Config="_config"></Chart>

@code {
    private const int InitalCount = 5;
    private PieConfig _config;

    protected override void OnInitialized()
    {
        _config = new PieConfig
            {
                Options = new PieOptions
                {
                    Responsive = true,
                    Title = new OptionsTitle
                    {
                        Display = true,
                        Text = "ChartJs.Blazor Pie Chart"
                    }
                }
            };

        int[] arrdata = Laga.Numbers.Rand.Integers(10, 2, 10);

        foreach (string color in new[] { "Red", "Yellow", "Green", "Blue" })
        {
            //_config.Data.Labels.Add(color);
            _config.Data.Labels.Add(arrdata.ToString());
        }

        //int[] arrdata = Laga.Numbers.Rand.Integers(4, 2, 10);
        
        PieDataset<int> dataset = new PieDataset<int>(arrdata) //new PieDataset<int>(new[] { 6, 5, 3, 7 })
            {
                BackgroundColor = Laga.Numbers.ColorRange.Generate("Viridis", 10).ToString(),
                //BackgroundColor = new[]
                //{
                    //ColorUtil.ColorHexString(255, 99, 132), // Slice 1 aka "Red"
                    //ColorUtil.ColorHexString(255, 205, 86), // Slice 2 aka "Yellow"
                    //ColorUtil.ColorHexString(75, 192, 192), // Slice 3 aka "Green"
                    //ColorUtil.ColorHexString(54, 162, 235), // Slice 4 aka "Blue"
               //}
            };

        _config.Data.Datasets.Add(dataset);
    }
}
